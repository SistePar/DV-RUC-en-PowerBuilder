# DV RUC en PowerBuilder

Calcular el Digito Verificador de un RUC según la SET de Paraguay

En PowerBuilder se llama directamente a la función: `fn_calcular_dv('0000001')` donde devuelve el DV como resultado.
